const supabase = require('../../db.js');
const express = require('express');
const router = express.Router();
const controoler = require('../controller/authController')
const app = express();

app.get("/auth/cadastro", );

module.exports = router;